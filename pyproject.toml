[project]
name = "lys-bbb"
version = "0.1.0"
description = "Staged BBB leakage MRI processing utilities for Bruker mouse MRI data."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
